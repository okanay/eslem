import { HeroBgImage } from '@/components/index/hero/hero-bg-image';

export const Hero = () => {
  return (
    <div className={'relative h-[400px] w-full lg:h-[640px]'}>
      <HeroBgImage />
    </div>
  );
};
